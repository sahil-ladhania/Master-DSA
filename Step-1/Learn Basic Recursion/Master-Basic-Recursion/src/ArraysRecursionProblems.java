import java.util.Arrays;

public class ArraysRecursionProblems {
    public static void main(String[] args) {
        System.out.println("Basic Arrays Questions on Recursion :- ");
        int[] arr1 = {1,2,3,4,5};
        int answer1 = sumOfArray(arr1);
        System.out.println("Answer 1 : " + Arrays.toString(arr1));
        int[] arr2 = {1,2,3,4,5};
        int answer2 = productOfArray(arr2);
        System.out.println("Answer 2 : " + Arrays.toString(arr2));
        int[] arr3 = {1,2,3,4,5,1};
        int x  = 1;
        int answer3 = countOccurences(x, arr3);
        System.out.println("Answer 3 : " + Arrays.toString(arr3));
        int[] arr4 = {1,2,3,4,5};
        int answer4 = maxElement(arr4);
        System.out.println("Answer 4 : " + Arrays.toString(arr4));
        int[] arr5 = {1,2,3,4,5};
        int answer5 = reverseArray(arr5);
        System.out.println("Answer 5 : " + Arrays.toString(arr5));
    }

    // Array Sum: Write a recursive function to find the sum of all elements in an array.
    public static int sumOfArray(int[] arr) {
        // Base Case
        if(true){
            return 1;
        }
        else{
            // Recursive Relation
            // Processing
        }
        return 1;
    }

    // Array Product: Write a recursive function to find the product of all elements in an array.
    public static int productOfArray(int[] arr) {
        // Base Case
        if(true){
            return 1;
        }
        else{
            // Recursive Relation
            // Processing
        }
        return 1;
    }

    // Count Occurrences in Array: Write a recursive function to count the number of times a given element appears in an array.
    public static int countOccurences(int x , int[] arr) {
        // Base Case
        if(true){
            return 1;
        }
        else{
            // Recursive Relation
            // Processing
        }
        return 1;
    }

    // Maximum Element in Array: Write a recursive function to find the maximum element in an array.
    public static int maxElement(int[] arr) {
        // Base Case
        if(true){
            return 1;
        }
        else{
            // Recursive Relation
            // Processing
        }
        return 1;
    }

    // Reverse Array: Write a recursive function to reverse an array.
    public static int reverseArray(int[] arr) {
        // Base Case
        if(true){
            return 1;
        }
        else{
            // Recursive Relation
            // Processing
        }
        return 1;
    }

}
