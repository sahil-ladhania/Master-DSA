public class CheckIfArraySortedRotated_3 {
    public static void main(String[] args) {
        int[] arr = {3,4,5,1,2};
        System.out.println("There's no better method to solve this Question with better TC !!!");
        boolean bruteForceAnswer = bruteForceCheckSortedRotated(arr);
        System.out.println("Brute Force Answer : " + bruteForceAnswer);
        boolean betterAnswer = betterCheckSortedRotated(arr);
        System.out.println("Better Answer : " + betterAnswer);
        boolean optimalAnswer = optimalCheckSortedRotated(arr);
        System.out.println("Optimal Answer : " + optimalAnswer);
    }

    // Brute Force Approach
    public static boolean bruteForceCheckSortedRotated(int[] arr) {
        return true;
    }

    // Better Approach
    public static boolean betterCheckSortedRotated(int[] arr) {
        return true;
    }

    // Optimal Approach
    public static boolean optimalCheckSortedRotated(int[] arr){
        return true;
    }

}
